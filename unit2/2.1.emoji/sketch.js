function setup() {
  // create a canvas
  createCanvas(400, 400);

  // disable animation
  noLoop();
}

function draw() {
  background(220);
  fill("yellow");
  circle(200,200,250);
  stroke("Black");
  

  rectMode(CENTER);
 
}
