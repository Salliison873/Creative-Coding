function setup() {
  // create a canvas
  createCanvas(400, 400);

  // disable animation
  noLoop();
}

function draw() {
  //Body of the face
  background(220);
  fill("yellow");
  circle(200,200,250);
  stroke("Black");

  //The mounth
  fill("black");
  strokeweight(1);
  ellpse(100,50,10,50);
  
  
  

  rectMode(CENTER);
 
}
